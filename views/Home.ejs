<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shoe Store</title>
</head>

<body>
  <h1>Shoe Store</h1>
  <a href="/shoes/add">Add</a>
  <table>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Category</th>
      <th>Size</th>
      <th>Status</th>
      <th>Action</th>
    </tr>
    <% data.forEach(element => { %>
    <tr>
      <td><%= element.id %></td>
      <td><%= element.name %></td>
      <td><%= element.categoryId %></td>
      <td><%= element.minSize %>-<%= element.maxSize %></td>
      <td><%= element.status %></td>
      <% if (element.status==="Available") { %>
      <td><a href="/shoes/status/:id/available">Available</a></td>
      <% } %>
      <%  if (element.status==="Discontinued") { %>
      <td><a href="/shoes/status/:id/discontinue">Discontinue</a></td>
      <% } %>
      <td><a href="/shoes/delete/:id"></a>Delete</td>
    </tr>
    <% }) %>
  </table>
</body>

</html>